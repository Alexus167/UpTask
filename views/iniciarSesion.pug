doctype html
html(lang="en")
    head
        include partials/head
    body.login
        .contenedor-formulario 
            h1 Iniciar sesión en Uptask 
            
            if locals.mensajes 
                .alertas 
                    each categoria in Object.keys(locals.mensajes)
                        each error in mensajes[categoria]
                            .alerta(class=`${categoria}`)
                                p= error            

            form#formulario.caja.caja-login(method="POST" action="/iniciarSesion")
                .campo 
                    label(for="email") E-mail:
                    input(type="email" name="email" placeholder="Tu Email" value= email)
                .campo 
                    label(for="password") Password:
                    input(type="password" name="password" placeholder="Tu Password" value= password)
                .campo.enviar
                    input.boton(type="submit" value="Iniciar sesión")   
                .campo.acciones 
                    a(href="/crearCuenta") ¿No tenes cuenta? Crea una gratis
                    a(href="/reestablecer") ¿Olvidaste tu contraseña?            

